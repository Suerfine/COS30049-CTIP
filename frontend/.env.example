EXPO_PUBLIC_API_HOST=localhost
EXPO_PUBLIC_API_PORT=5000
EXPO_PUBLIC_API_PROTOCOL=http

# Set to false to use HTTP instead of HTTPS for the dummy API server (port 4000).
# Must match HTTPS_ENABLED in backend/.env.
# Defaults to true if omitted.
EXPO_PUBLIC_HTTPS_ENABLED=true
